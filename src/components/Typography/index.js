import React from "react";
import "./style.css";

const Typography = (props) => {

    return (
        <p className="main-typography">{props.title}</p>
    )

}

export default Typography;